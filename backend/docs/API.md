# MomentsShare API 接口文档

本文档详细描述了 MomentsShare 项目的所有 RESTful API 接口。

---

## 🌐 在线 API 文档

项目集成了 **drf-spectacular**，提供交互式 API 文档界面：

| 文档类型 | URL | 说明 |
|----------|-----|------|
| Swagger UI | `/api/docs/` | 可交互的 API 测试界面 |
| ReDoc | `/api/redoc/` | 美观的只读文档 |
| OpenAPI Schema | `/api/schema/` | OpenAPI 3.0 JSON/YAML |

### 访问方式

启动服务后访问：

- **Swagger UI**: http://localhost:8000/api/docs/
- **ReDoc**: http://localhost:8000/api/redoc/
- **OpenAPI Schema**: http://localhost:8000/api/schema/

### 在 Swagger UI 中测试 API

1. 打开 http://localhost:8000/api/docs/
2. 点击右上角 **Authorize** 按钮
3. 输入 `Bearer <your_access_token>`
4. 点击 **Authorize** 确认
5. 现在可以测试需要认证的接口了

---

## 目录

1. [认证接口](#1-认证接口)
2. [用户接口](#2-用户接口)
3. [动态接口](#3-动态接口)
4. [好友接口](#4-好友接口)
5. [评论接口](#5-评论接口)
6. [AI 服务接口](#6-ai-服务接口)
7. [管理后台接口](#7-管理后台接口)

---

## 通用说明

### 基础 URL

```
http://your-domain.com/api/v1/
```

### 认证方式

除注册和登录接口外，所有接口均需要 JWT Token 认证：

```http
Authorization: Bearer <access_token>
```

### 响应格式

**成功响应**:
```json
{
  "field": "value"
}
```

**错误响应**:
```json
{
  "detail": {
    "field": ["错误信息"]
  }
}
```

**分页响应**:
```json
{
  "count": 100,
  "next": "http://api/endpoint/?page=2",
  "previous": null,
  "results": [...]
}
```

### HTTP 状态码

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 201 | 创建成功 |
| 204 | 删除成功 |
| 400 | 请求参数错误 |
| 401 | 未认证 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

## 1. 认证接口

### 1.1 用户注册

**POST** `/api/v1/auth/register/`

**权限**: 无需认证

**请求体**:
```json
{
  "phone": "13812345678",
  "username": "testuser",
  "nickname": "测试用户",
  "password": "securePassword123",
  "avatar": "<file>" // 可选
}
```

**字段说明**:
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| phone | string | 是 | 11位手机号，唯一 |
| username | string | 是 | 用户名，30字符以内，唯一 |
| nickname | string | 是 | 昵称，30字符以内 |
| password | string | 是 | 密码，需符合安全规范 |
| avatar | file | 否 | 头像图片 |

**成功响应** (201):
```json
{
  "id": 1,
  "phone": "13812345678",
  "username": "testuser",
  "nickname": "测试用户",
  "avatar": "/media/avatars/xxx.jpg"
}
```

**错误响应** (400):
```json
{
  "detail": {
    "phone": ["该手机号已注册"],
    "username": ["用户名已存在"]
  }
}
```

---

### 1.2 用户登录

**POST** `/api/v1/auth/login/`

**权限**: 无需认证

**请求体**:
```json
{
  "phone": "13812345678",
  "password": "securePassword123"
}
```

**成功响应** (200):
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user_info": {
    "id": 1,
    "nickname": "测试用户",
    "avatar": "/media/avatars/xxx.jpg"
  }
}
```

**错误响应** (400):
```json
{
  "detail": {
    "detail": ["手机号或密码错误"]
  }
}
```

---

### 1.3 用户登出

**POST** `/api/v1/auth/logout/`

**权限**: 需要认证

**请求体**:
```json
{
  "refresh_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..."
}
```

**成功响应** (200):
```json
{
  "detail": "已退出登录"
}
```

**错误响应** (400):
```json
{
  "detail": "refresh_token 无效"
}
```

---

## 2. 用户接口

### 2.1 获取当前用户信息

**GET** `/api/v1/users/me/`

**权限**: 需要认证

**成功响应** (200):
```json
{
  "id": 1,
  "phone": "13812345678",
  "username": "testuser",
  "nickname": "测试用户",
  "avatar": "/media/avatars/xxx.jpg",
  "created_at": "2024-01-01T12:00:00+08:00"
}
```

---

### 2.2 更新当前用户信息

**PUT/PATCH** `/api/v1/users/me/`

**权限**: 需要认证

**请求体**:
```json
{
  "username": "newusername",
  "nickname": "新昵称",
  "avatar": "<file>"
}
```

**可更新字段**:
| 字段 | 类型 | 说明 |
|------|------|------|
| username | string | 用户名（唯一） |
| nickname | string | 昵称 |
| avatar | file | 头像图片 |

**成功响应** (200):
```json
{
  "id": 1,
  "phone": "13812345678",
  "username": "newusername",
  "nickname": "新昵称",
  "avatar": "/media/avatars/new.jpg",
  "created_at": "2024-01-01T12:00:00+08:00"
}
```

---

### 2.3 更换手机号

**POST** `/api/v1/users/me/phone/`

**权限**: 需要认证

**请求体**:
```json
{
  "password": "currentPassword123",
  "new_phone": "13987654321"
}
```

**成功响应** (200):
```json
{
  "detail": "手机号已更新"
}
```

**错误响应** (400):
```json
{
  "detail": {
    "password": ["密码验证失败"],
    "new_phone": ["该手机号已注册"]
  }
}
```

---

## 3. 动态接口

### 3.1 发布动态

**POST** `/api/v1/moments/`

**权限**: 需要认证

**Content-Type**: `multipart/form-data`

**请求参数**:
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| content | string | 否 | 文字内容 |
| type | string | 是 | 动态类型: `IMAGE` 或 `VIDEO` |
| images | file[] | 条件 | 图片文件（IMAGE类型必填，最多9张） |
| video | file | 条件 | 视频文件（VIDEO类型必填） |
| labels | string[] | 否 | 标签列表，每个最多10字符 |

**图文动态请求示例**:
```
POST /api/v1/moments/
Content-Type: multipart/form-data

content: 今天天气真好
type: IMAGE
images: [<file1>, <file2>]
labels: ["日常", "生活"]
```

**视频动态请求示例**:
```
POST /api/v1/moments/
Content-Type: multipart/form-data

content: 分享一段视频
type: VIDEO
video: <video_file>
labels: ["视频", "分享"]
```

**成功响应** (201):
```json
{
  "id": 1,
  "content": "今天天气真好",
  "type": "IMAGE"
}
```

**错误响应** (400):
```json
{
  "detail": {
    "content": ["包含敏感内容"],
    "images": ["图片最多9张"],
    "video": ["视频动态需要上传视频文件"]
  }
}
```

---

### 3.2 获取动态详情

**GET** `/api/v1/moments/{id}/`

**权限**: 需要认证

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| id | integer | 动态 ID |

**成功响应** (200):
```json
{
  "id": 1,
  "author": {
    "id": 1,
    "nickname": "测试用户",
    "avatar": "/media/avatars/xxx.jpg"
  },
  "content": "今天天气真好",
  "type": "IMAGE",
  "video_file": null,
  "video_status": "READY",
  "is_deleted": false,
  "created_at": "2024-01-01T12:00:00+08:00",
  "images": [
    {
      "id": 1,
      "image_file": "/media/images/xxx.jpg",
      "order": 1
    }
  ],
  "tags": [
    {"id": 1, "name": "日常"},
    {"id": 2, "name": "生活"}
  ]
}
```

**错误响应** (403):
```json
{
  "detail": "内容已删除"
}
```

---

### 3.3 好友动态信息流

**GET** `/api/v1/moments/feed/`

**权限**: 需要认证

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| page | integer | 页码（默认1） |

**成功响应** (200):
```json
{
  "count": 50,
  "next": "http://api/v1/moments/feed/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "author": {
        "id": 2,
        "nickname": "好友1",
        "avatar": "/media/avatars/xxx.jpg"
      },
      "content": "朋友的动态",
      "type": "IMAGE",
      "video_file": null,
      "video_status": "READY",
      "is_deleted": false,
      "created_at": "2024-01-01T12:00:00+08:00",
      "images": [],
      "tags": []
    }
  ]
}
```

**说明**:
- 只返回已接受好友的动态
- 不返回已删除的动态
- 视频动态只返回处理完成的
- 按创建时间倒序排列
- 每页10条

---

### 3.4 我的动态列表

**GET** `/api/v1/moments/my/`

**权限**: 需要认证

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| page | integer | 页码（默认1） |

**成功响应** (200):
```json
{
  "count": 20,
  "next": "http://api/v1/moments/my/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "author": {...},
      "content": "我的动态内容",
      "type": "IMAGE",
      ...
    }
  ]
}
```

**说明**:
- 只返回当前用户发布的动态
- 不返回已删除的动态
- 按创建时间倒序排列

---

### 3.5 搜索动态

**GET** `/api/v1/moments/search/`

**权限**: 需要认证

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| keyword | string | 关键词（支持中文和拼音匹配） |
| label | string | 标签名称（支持拼音匹配） |
| start_date | string | 开始日期 (ISO格式: 2024-01-01) |
| end_date | string | 结束日期 (ISO格式: 2024-01-31) |
| page | integer | 页码（默认1） |

**请求示例**:
```
GET /api/v1/moments/search/?keyword=天气&label=日常&start_date=2024-01-01&end_date=2024-01-31
# 拼音搜索示例
GET /api/v1/moments/search/?keyword=tq
GET /api/v1/moments/search/?keyword=tianqi
```

**成功响应** (200):
```json
{
  "count": 5,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 1,
      "author": {...},
      "content": "今天天气真好",
      "type": "IMAGE",
      ...
    }
  ]
}
```

**说明**:
- 支持中文直接匹配
- 支持拼音全拼匹配（如 `tianqi` 匹配 "天气"）
- 支持拼音首字母匹配（如 `tq` 匹配 "天气"）

---

### 3.6 搜索建议

**GET** `/api/v1/moments/search/suggestions/`

**权限**: 需要认证

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| q | string | 搜索关键词（必填） |
| limit | integer | 返回建议数量限制（默认10） |

**成功响应** (200):
```json
{
  "suggestions": [
    {
      "text": "...今天天气真好...",
      "keyword": "tq",
      "moment_id": 1
    }
  ],
  "tags": [
    {"id": 1, "name": "天气"},
    {"id": 2, "name": "日常"}
  ]
}
```

**说明**:
- 返回匹配的动态内容片段
- 返回匹配的标签列表
- 支持拼音匹配

---

### 3.7 热门搜索

**GET** `/api/v1/moments/search/hot/`

**权限**: 需要认证

**成功响应** (200):
```json
{
  "tags": [
    {"id": 1, "name": "美食", "count": 50},
    {"id": 2, "name": "旅行", "count": 35},
    {"id": 3, "name": "日常", "count": 28}
  ]
}
```

**说明**:
- 返回使用频率最高的标签（最多10个）
- 包含每个标签的使用次数

---

## 4. 好友接口

### 4.1 发起好友申请

**POST** `/api/v1/friends/request/`

**权限**: 需要认证

**请求体**:
```json
{
  "to_user_id": 2
}
```

**成功响应** (201):
```json
{
  "id": 1,
  "from_user": 1,
  "to_user": 2,
  "status": "PENDING",
  "created_at": "2024-01-01T12:00:00+08:00"
}
```

**错误响应** (400):
```json
{
  "detail": "不能向自己发起申请"
}
```

```json
{
  "detail": "已发起过申请"
}
```

---

### 4.2 响应好友申请

**POST** `/api/v1/friends/respond/`

**权限**: 需要认证

**请求体**:
```json
{
  "request_id": 1,
  "action": "accept"  // 或 "reject"
}
```

**成功响应** (200):
```json
{
  "id": 1,
  "from_user": 2,
  "to_user": 1,
  "status": "ACCEPTED",
  "created_at": "2024-01-01T12:00:00+08:00"
}
```

**错误响应** (403):
```json
{
  "detail": "无权处理该申请"
}
```

**错误响应** (404):
```json
{
  "detail": "申请不存在"
}
```

---

### 4.3 获取好友列表

**GET** `/api/v1/friends/`

**权限**: 需要认证

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| page | integer | 页码（默认1） |

**成功响应** (200):
```json
{
  "count": 15,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 2,
      "phone": "13812345678",
      "username": "friend1",
      "nickname": "好友1",
      "avatar": "/media/avatars/xxx.jpg"
    }
  ]
}
```

---

### 4.4 获取待处理好友申请

**GET** `/api/v1/friends/pending/`

**权限**: 需要认证

**成功响应** (200):
```json
{
  "count": 3,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 1,
      "from_user": 2,
      "to_user": 1,
      "status": "PENDING",
      "created_at": "2024-01-01T12:00:00+08:00"
    }
  ]
}
```

**说明**:
- 返回当前用户收到的待处理好友申请
- 按创建时间倒序排列

---

### 4.5 搜索用户

**GET** `/api/v1/friends/search/`

**权限**: 需要认证

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| keyword | string | 搜索关键词（昵称或手机号） |
| page | integer | 页码（默认1） |

**成功响应** (200):
```json
{
  "count": 5,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 3,
      "phone": "13899999999",
      "username": "newuser",
      "nickname": "新用户",
      "avatar": "/media/avatars/xxx.jpg"
    }
  ]
}
```

**说明**:
- 用于添加好友前搜索用户
- 不返回当前用户自己
- 支持按昵称或手机号模糊搜索

---

### 4.6 删除好友

**DELETE** `/api/v1/friends/{user_id}/`

**权限**: 需要认证

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| user_id | integer | 好友用户 ID |

**成功响应** (204): 无内容

**错误响应** (403):
```json
{
  "detail": "未找到好友关系"
}
```

---

## 5. 评论接口

### 5.1 获取评论列表

**GET** `/api/v1/moments/{moment_id}/comments/`

**权限**: 需要认证

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| moment_id | integer | 动态 ID |

**成功响应** (200):
```json
{
  "count": 10,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 1,
      "moment": 1,
      "author": 1,
      "content": "这是一条评论",
      "parent": null,
      "created_at": "2024-01-01T12:00:00+08:00",
      "replies": [
        {
          "id": 2,
          "moment": 1,
          "author": 2,
          "content": "这是一条回复",
          "parent": 1,
          "created_at": "2024-01-01T12:01:00+08:00",
          "replies": []
        }
      ]
    }
  ]
}
```

**说明**:
- 只返回一级评论（parent 为 null）
- 回复通过 replies 字段嵌套返回
- 不返回已删除的评论

---

### 5.2 发布评论

**POST** `/api/v1/moments/{moment_id}/comments/`

**权限**: 需要认证

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| moment_id | integer | 动态 ID |

**请求体**:
```json
{
  "content": "这是一条评论",
  "parent_id": null  // 可选，填写表示回复某条评论
}
```

**发布一级评论**:
```json
{
  "content": "这是一条评论"
}
```

**回复评论**:
```json
{
  "content": "这是一条回复",
  "parent_id": 1
}
```

**成功响应** (201):
```json
{
  "id": 1,
  "moment": 1,
  "author": 1,
  "content": "这是一条评论",
  "parent": null,
  "created_at": "2024-01-01T12:00:00+08:00",
  "replies": []
}
```

**错误响应** (400):
```json
{
  "detail": {
    "parent_id": ["父评论不属于该动态"]
  }
}
```

**错误响应** (404):
```json
{
  "detail": "动态不存在或已被删除"
}
```

---

### 5.3 点赞/取消点赞

**POST** `/api/v1/moments/{moment_id}/like/`

**权限**: 需要认证

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| moment_id | integer | 动态 ID |

**成功响应 - 点赞** (201):
```json
{
  "detail": "点赞成功",
  "liked": true,
  "likes_count": 10,
  "like": {
    "id": 1,
    "moment": 1,
    "user": 1,
    "created_at": "2024-01-01T12:00:00+08:00"
  }
}
```

**成功响应 - 取消点赞** (200):
```json
{
  "detail": "取消点赞成功",
  "liked": false,
  "likes_count": 9
}
```

**说明**:
- 如果用户未点赞则点赞，已点赞则取消点赞
- 返回当前点赞状态和点赞总数

---

### 5.4 打分/评分

**POST** `/api/v1/interactions/rate/`

**权限**: 需要认证

**请求体**:
```json
{
  "moment_id": 1,
  "score": 5
}
```

**字段说明**:
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| moment_id | integer | 是 | 动态 ID |
| score | integer | 是 | 评分 (1-5) |

**成功响应** (201):
```json
{
  "id": 1,
  "moment": 1,
  "user": 1,
  "score": 5,
  "created_at": "2024-01-01T12:00:00+08:00"
}
```

**说明**:
- 如果用户已打分，则更新分数
- 分数范围 1-5

---

### 5.5 获取平均分

**GET** `/api/v1/interactions/avg_score/`

**权限**: 公开

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| moment_id | integer | 动态 ID（必填） |

**成功响应** (200):
```json
{
  "moment_id": 1,
  "avg_score": 4.5
}
```

---

## 6. AI 服务接口

### 6.1 文案润色 (MC-01)

**POST** `/api/v1/ai/polish/`

**权限**: 需要认证

**Content-Type**: `multipart/form-data`

**请求参数**:
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| text | string | 条件 | 原始文案（text 或 image 至少一个） |
| image | file | 条件 | 参考图片（text 或 image 至少一个） |

**请求示例 - 纯文字润色**:
```
POST /api/v1/ai/polish/
Content-Type: multipart/form-data

text: 今天吃了好吃的
```

**请求示例 - 图片+文字润色**:
```
POST /api/v1/ai/polish/
Content-Type: multipart/form-data

text: 今天吃了好吃的
image: <food_image.jpg>
```

**请求示例 - 仅图片生成文案**:
```
POST /api/v1/ai/polish/
Content-Type: multipart/form-data

image: <scenic_image.jpg>
```

**成功响应** (200):
```json
{
  "generated_text": "今天邂逅了一份令人垂涎的美食，每一口都是味蕾的盛宴！🍽️✨"
}
```

**错误响应** (400):
```json
{
  "detail": {
    "non_field_errors": ["text 或 image 至少提供一个"]
  }
}
```

**说明**:
- 如果 AI 服务不可用，会返回带 emoji 的简单润色结果
- 支持同时提供文字和图片，AI 会综合考虑生成文案

---

### 6.2 标签推荐 (MC-02)

**POST** `/api/v1/ai/recommend-tags/`

**权限**: 需要认证

**Content-Type**: `multipart/form-data`

**请求参数**:
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| text | string | 条件 | 内容文字（text 或 image 至少一个） |
| image | file | 条件 | 参考图片（text 或 image 至少一个） |

**请求示例**:
```
POST /api/v1/ai/recommend-tags/
Content-Type: multipart/form-data

text: 今天去了咖啡馆，点了一杯拿铁
image: <coffee_image.jpg>
```

**成功响应** (200):
```json
{
  "tags": ["咖啡", "下午茶", "拿铁", "休闲时光", "惬意"]
}
```

**错误响应** (400):
```json
{
  "detail": {
    "non_field_errors": ["text 或 image 至少提供一个"]
  }
}
```

**说明**:
- 返回 3-5 个推荐标签
- 每个标签最多 10 个字符
- 如果 AI 服务不可用，会返回默认标签：["日常", "分享", "生活"]

---

## 7. 管理后台接口

### 7.1 管理员登录

**POST** `/api/v1/admin/auth/login/`

**权限**: 无需认证

**请求体**:
```json
{
  "phone": "13800000000",
  "password": "adminPassword123"
}
```

**成功响应** (200):
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user_info": {
    "id": 1,
    "nickname": "管理员",
    "avatar": null
  }
}
```

**错误响应** (400):
```json
{
  "detail": {
    "detail": ["非管理员账号禁止访问"]
  }
}
```

**说明**:
- 只有 `is_staff=True` 或 `is_superuser=True` 的用户可以登录

---

### 7.2 获取内容列表

**GET** `/api/v1/admin/contents/`

**权限**: 管理员

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| user_id | integer | 按用户过滤（可选） |
| page | integer | 页码（默认1） |

**请求示例**:
```
GET /api/v1/admin/contents/?user_id=1&page=1
```

**成功响应** (200):
```json
{
  "count": 100,
  "next": "http://api/v1/admin/contents/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "author": {
        "id": 1,
        "nickname": "用户1",
        "avatar": null
      },
      "content": "动态内容",
      "type": "IMAGE",
      "video_file": null,
      "video_status": "READY",
      "is_deleted": false,
      "created_at": "2024-01-01T12:00:00+08:00",
      "images": [],
      "tags": []
    }
  ]
}
```

**说明**:
- 返回所有动态，包括已删除的
- 按创建时间倒序排列

---

### 7.3 下架内容

**DELETE** `/api/v1/admin/contents/{pk}/`

**权限**: 管理员

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| pk | integer | 动态 ID |

**成功响应** (200):
```json
{
  "detail": "已下架"
}
```

**错误响应** (404):
```json
{
  "detail": "动态不存在"
}
```

**说明**:
- 软删除，设置 `is_deleted=True`
- 下架后用户端不可见

---

### 7.4 获取统计数据

**GET** `/api/v1/admin/stats/`

**权限**: 管理员

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| days | integer | 统计天数（默认7） |

**成功响应** (200):
```json
{
  "daily_stats": [
    {
      "date": "2024-01-01",
      "dau": 150,
      "daily_new_users": 20,
      "daily_posts": 85
    },
    {
      "date": "2024-01-02",
      "dau": 165,
      "daily_new_users": 15,
      "daily_posts": 92
    }
  ],
  "content_distribution": [
    {"type": "图片动态", "count": 500},
    {"type": "视频动态", "count": 120}
  ]
}
```

**字段说明**:
| 字段 | 说明 |
|------|------|
| daily_stats | 每日统计数据数组 |
| daily_stats[].date | 日期 |
| daily_stats[].dau | 日活跃用户数（当日发帖或评论的用户） |
| daily_stats[].daily_new_users | 当日新注册用户数 |
| daily_stats[].daily_posts | 当日发帖数（不含已删除） |
| content_distribution | 内容类型分布统计 |

---

### 7.5 用户列表

**GET** `/api/v1/admin/users/`

**权限**: 管理员

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| search | string | 搜索关键词（手机号或昵称） |
| page | integer | 页码（默认1） |

**成功响应** (200):
```json
{
  "count": 100,
  "next": "http://api/v1/admin/users/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "phone": "13812345678",
      "username": "testuser",
      "nickname": "测试用户",
      "avatar": "/media/avatars/xxx.jpg",
      "is_active": true,
      "is_staff": false,
      "created_at": "2024-01-01T12:00:00+08:00"
    }
  ]
}
```

---

### 7.6 封禁/解封用户

**POST** `/api/v1/admin/users/{pk}/status/`

**权限**: 管理员

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| pk | integer | 用户 ID |

**成功响应** (200):
```json
{
  "detail": "用户已封禁",
  "is_active": false
}
```

或

```json
{
  "detail": "用户已解封",
  "is_active": true
}
```

**说明**:
- 切换用户的 is_active 状态
- 封禁后用户无法登录

---

### 7.7 评论列表

**GET** `/api/v1/admin/comments/`

**权限**: 管理员

**查询参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| user_id | integer | 按用户ID过滤 |
| moment_id | integer | 按动态ID过滤 |
| keyword | string | 搜索评论内容 |
| page | integer | 页码（默认1） |

**成功响应** (200):
```json
{
  "count": 50,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 1,
      "moment": 1,
      "author": {
        "id": 1,
        "nickname": "用户1"
      },
      "content": "评论内容",
      "parent": null,
      "created_at": "2024-01-01T12:00:00+08:00",
      "is_deleted": false
    }
  ]
}
```

---

### 7.8 删除评论

**DELETE** `/api/v1/admin/comments/{pk}/`

**权限**: 管理员

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| pk | integer | 评论 ID |

**成功响应** (204): 无内容

**说明**:
- 物理删除评论（非软删除）

---

## 📋 API 路由汇总

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| **认证** ||||
| POST | `/api/v1/auth/register/` | 用户注册 | 公开 |
| POST | `/api/v1/auth/login/` | 用户登录 | 公开 |
| POST | `/api/v1/auth/logout/` | 用户登出 | 认证 |
| **用户** ||||
| GET | `/api/v1/users/me/` | 获取当前用户 | 认证 |
| PUT/PATCH | `/api/v1/users/me/` | 更新用户信息 | 认证 |
| POST | `/api/v1/users/me/phone/` | 更换手机号 | 认证 |
| **动态** ||||
| POST | `/api/v1/moments/` | 发布动态 | 认证 |
| GET | `/api/v1/moments/{id}/` | 动态详情 | 认证 |
| GET | `/api/v1/moments/feed/` | 好友动态流 | 认证 |
| GET | `/api/v1/moments/my/` | 我的动态列表 | 认证 |
| GET | `/api/v1/moments/search/` | 搜索动态 | 认证 |
| GET | `/api/v1/moments/search/suggestions/` | 搜索建议 | 认证 |
| GET | `/api/v1/moments/search/hot/` | 热门搜索 | 认证 |
| **好友** ||||
| GET | `/api/v1/friends/` | 好友列表 | 认证 |
| POST | `/api/v1/friends/request/` | 发起好友申请 | 认证 |
| POST | `/api/v1/friends/respond/` | 响应好友申请 | 认证 |
| GET | `/api/v1/friends/pending/` | 待处理好友申请 | 认证 |
| GET | `/api/v1/friends/search/` | 搜索用户 | 认证 |
| DELETE | `/api/v1/friends/{user_id}/` | 删除好友 | 认证 |
| **互动** ||||
| GET | `/api/v1/moments/{moment_id}/comments/` | 评论列表 | 认证 |
| POST | `/api/v1/moments/{moment_id}/comments/` | 发布评论 | 认证 |
| POST | `/api/v1/moments/{moment_id}/like/` | 点赞/取消点赞 | 认证 |
| POST | `/api/v1/interactions/rate/` | 打分评分 | 认证 |
| GET | `/api/v1/interactions/avg_score/` | 获取平均分 | 公开 |
| **AI** ||||
| POST | `/api/v1/ai/polish/` | 文案润色 | 认证 |
| POST | `/api/v1/ai/recommend-tags/` | 标签推荐 | 认证 |
| **管理** ||||
| POST | `/api/v1/admin/auth/login/` | 管理员登录 | 公开 |
| GET | `/api/v1/admin/contents/` | 内容列表 | 管理员 |
| DELETE | `/api/v1/admin/contents/{pk}/` | 下架内容 | 管理员 |
| GET | `/api/v1/admin/users/` | 用户列表 | 管理员 |
| POST | `/api/v1/admin/users/{pk}/status/` | 封禁/解封用户 | 管理员 |
| GET | `/api/v1/admin/comments/` | 评论列表 | 管理员 |
| DELETE | `/api/v1/admin/comments/{pk}/` | 删除评论 | 管理员 |
| GET | `/api/v1/admin/stats/` | 统计数据 | 管理员 |

---

## 🔧 错误码参考

| 错误信息 | 说明 | 接口 |
|----------|------|------|
| 该手机号已注册 | 手机号重复 | 注册 |
| 用户名已存在 | 用户名重复 | 注册/更新 |
| 手机号或密码错误 | 登录凭证错误 | 登录 |
| 账号未激活 | 用户被禁用 | 登录 |
| 缺少 refresh_token | 登出参数缺失 | 登出 |
| refresh_token 无效 | Token 已过期或无效 | 登出 |
| 密码验证失败 | 当前密码错误 | 换绑手机 |
| 包含敏感内容 | 内容触发敏感词 | 发布动态 |
| 图片最多9张 | 超出图片数量限制 | 发布动态 |
| 视频动态需要上传视频文件 | 缺少视频文件 | 发布动态 |
| 内容已删除 | 访问已删除内容 | 动态详情 |
| 视频处理中 | 视频尚未转码完成 | 动态详情 |
| 不能向自己发起申请 | 自己添加自己 | 好友申请 |
| 已发起过申请 | 重复申请 | 好友申请 |
| 无权处理该申请 | 非接收方处理 | 好友响应 |
| 未找到好友关系 | 不是好友 | 删除好友 |
| 动态不存在或已被删除 | 无效动态 | 发布评论 |
| 父评论不属于该动态 | 回复目标错误 | 发布评论 |
| text 或 image 至少提供一个 | AI参数缺失 | AI 接口 |
| 非管理员账号禁止访问 | 权限不足 | 管理员登录 |

